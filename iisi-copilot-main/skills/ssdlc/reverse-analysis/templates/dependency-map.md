# 依賴關係圖：{模組名稱}

**產生日期**：{date}
**模組路徑**：{module_path}

---

## 內部依賴圖

```mermaid
graph TD
    A[{進入點}] --> B[{服務層}]
    B --> C[{資料存取層}]
    B --> D[{外部服務}]
    C --> E[{資料庫}]
```

## 外部依賴清單

| 依賴名稱     | 類型               | 版本      | 用途    | 風險     |
| ------------ | ------------------ | --------- | ------- | -------- |
| {dependency} | Library/Service/DB | {version} | {usage} | 高/中/低 |

## 循環依賴分析

> 循環依賴會增加耦合度，應盡量消除。

| 循環路徑  | 嚴重程度 | 建議拆解方式     |
| --------- | -------- | ---------------- |
| A → B → A | 高       | 引入介面或中介層 |

## 依賴矩陣

| 模組    | {模組A} | {模組B} | {模組C} |
| ------- | ------- | ------- | ------- |
| {模組A} | -       | ✓       | ✗       |
| {模組B} | ✗       | -       | ✓       |
| {模組C} | ✗       | ✗       | -       |

> ✓ = 依賴，✗ = 不依賴
